---
import Item from '~/components/blog/ListItem.astro';
import type { Post } from '~/types';

export interface Props {
  posts: Array<Post>;
}

const { posts } = Astro.props;
---

<ul class="grid md:grid-cols-4 gap-8">
  {
    posts.flatMap((post) => (
      <li class="hover:bg-gray-100 rounded-lg border border-gray-200 ">
        <Item post={post} />
      </li>
    ))
  }
</ul>
