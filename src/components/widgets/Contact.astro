---
import FormContainer from '~/components/ui/Form.astro';
import Headline from '~/components/ui/Headline.astro';
import WidgetWrapper from '~/components/ui/WidgetWrapper.astro';
import { Contact } from '~/types';

const {
  title = await Astro.slots.render('title'),
  subtitle = await Astro.slots.render('subtitle'),
  tagline = await Astro.slots.render('tagline'),
  inputs,
  boxinputs,
  boxinputs2,
  textarea,
  disclaimer,
  question,
  button,
  description,

} = Astro.props as Contact;
---

<WidgetWrapper  containerClass={"max-w-3xl"}>
  <Headline title={title} subtitle={subtitle} tagline={tagline} />

  {
    inputs && (
      <div class="flex flex-col max-w-xl mx-auto rounded-lg backdrop-blur border border-gray-200 bg-white shadow p-4 sm:p-6 lg:p-8 w-full">
        <FormContainer
          inputs={inputs}
          boxinputs={boxinputs}
          boxinputs2={boxinputs2}
          textarea={textarea}
          disclaimer={disclaimer}
          question={question}
          button={button}
          description={description}
        />
      </div>
    )
  }
</WidgetWrapper>
