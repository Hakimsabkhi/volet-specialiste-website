---
import FormContainer from '~/components/ui/FormDevis.astro';
import Headline from '~/components/ui/Headline.astro';
import WidgetWrapper from '~/components/ui/WidgetWrapper.astro';
import { Devis } from '~/types';

const {
  title = await Astro.slots.render('title'),
  subtitle = await Astro.slots.render('subtitle'),
  tagline = await Astro.slots.render('tagline'),
  inputs,
  textarea,
  disclaimer,
  question,
  button,
  description,
} = Astro.props as Devis;
---

<WidgetWrapper containerClass={'max-w-7xl mx-auto'}>
  <Headline title={title} subtitle={subtitle} tagline={tagline} />

  {
    inputs && (
      <div class="flex flex-col max-w-4xl mx-auto rounded-lg backdrop-blur border border-gray-200 bg-white shadow p-4 sm:p-6 lg:p-8 w-full">
        <FormContainer
          inputs={inputs}
          textarea={textarea}
          disclaimer={disclaimer}
          question={question}
          button={button}
          description={description}
        />
      </div>
    )
  }
</WidgetWrapper>
