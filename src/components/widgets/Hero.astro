---
import { Icon } from 'astro-icon/components';
import Image from '~/components/common/Image.astro';

const {
  title = await Astro.slots.render('title'),
  subtitle = await Astro.slots.render('subtitle'),
  tagline,
  content = await Astro.slots.render('content'),
  callToAction = await Astro.slots.render('callToAction'),
  callToAction2 = await Astro.slots.render('callToAction2'),
  image = await Astro.slots.render('image'),
} = Astro.props;
---

<section class="relative md:-mt-[76px] not-prose">
  <div class="relative max-w-3xl px-4 sm:px-6">
    <div class="pt-0 md:pt-[76px] pointer-events-none"></div>
    <div class="mt-10 py-1 md:py-1">
      <div class="pb-10 max-w-screen-lg">
        {
          tagline && (
            <p
              class="text-base text-secondary dark:text-blue-200 font-bold tracking-wide uppercase"
              set:html={tagline}
            />
          )
        }
        {
          title && (
            <h1
              class="text-xl md:text-3xl font-bold leading-tighter tracking-normal mb-4 font-heading dark:text-gray-200"
              set:html={title}
            />
          )
        }
        <div class="max-w-3xl ">
          {subtitle && <p class=" text-base mb-6 dark:text-gray-200" set:html={subtitle} />}
          <div class="max-w-xs sm:max-w-md m-auto flex flex-nowrap flex-col sm:flex-row sm:justify-center gap-4">
            {
              callToAction && (
                <div class="flex w-full sm:w-auto">
                  {typeof callToAction === 'string' ? (
                    <Fragment set:html={callToAction} />
                  ) : (
                    <a class="btn btn-primary sm:mb-0 w-full" href={callToAction?.href} target="_blank" rel="noopener">
                      {callToAction?.icon && <></>}
                      {callToAction?.text}
                    </a>
                  )}
                </div>
              )
            }
            {
              callToAction2 && (
                <div class="flex w-full sm:w-auto">
                  {typeof callToAction2 === 'string' ? (
                    <Fragment set:html={callToAction2} />
                  ) : (
                    <a class="btn w-full" href={callToAction2?.href}>
                      {callToAction2?.icon && (
                        <>
                          <Icon name={callToAction2.icon} class="w-5 h-5 mr-1 -ml-1.5" />
                          &nbsp;
                        </>
                      )}
                      {callToAction2.text}
                    </a>
                  )}
                </div>
              )
            }
          </div>
        </div>

        {content && <Fragment set:html={content} />}
      </div>
      <div>
        {
          image && (
            <div class="m-auto max-w-5xl">
              {typeof image === 'string' ? (
                <Fragment set:html={image} />
              ) : (
                <Image
                  class=" rounded-md w-full"
                  widths={[400, 768, 1024, 2040]}
                  sizes="(max-width: 767px) 400px, (max-width: 1023px) 768px, (max-width: 2039px) 1024px, 2040px"
                  loading="eager"
                  width={800}
                  height={600}
                  {...image}
                />
              )}
            </div>
          )
        }
  </div>
  <div class="">
    <div>
        <p class="mt-5 ml-4 mb-6 text-xl uppercase font-bold">Sommaire:</p>
        <ul class="text-gray-500 dark:text-gray-400 font-sm ml-4 mb-2">
            <li class="">
                <a href="#section1" class="hover:underline"> 1. Explorez les différents types de volets roulants</a>
            </li>
            <li>
                <a href="#section2" class="hover:underline">2. Méthode de commande des volets roulants</a>
            </li>
            <li>
              <a href="#section3" class="hover:underline">3. Quel est le coût des volets roulants ?</a>
          </li>
          <li>
            <a href="#section4" class="hover:underline">4. Foire aux questions sur les volets roulants</a>
        </li>
        </ul>
    </div>
 </div>

<aside id="logo-sidebar" class="fixed top-20 right-20 w-80 h-screen transition-transform -translate-x-full sm:translate-x-0" aria-label="Sidebar">
  <div class="h-full px-3 py-4 overflow-y-auto text-center bg-gray-50 dark:bg-gray-800">
    <p class="mt-5 mb-6 text-xl  uppercase font-bold">Savoir plus:</p>
     <ul class="space-y-2 font-medium">
      
        <li>
          <a href="/reparation-volet-roulant" class=" flex items-center text-blod p-2 text-gray-900 rounded-lg dark:text-white hover:bg-gray-100 dark:hover:bg-gray-700 group">Comment Réparer un Volet Roulant</a>
        </li>
     </ul>
  </div>
</aside>
</div>
</section>
